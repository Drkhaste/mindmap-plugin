/**
 * MCP Accordion
 *
 * Custom animation logic has been removed as per user request to use simple,
 * instant toggling for better performance and to eliminate visual glitches.
 * The browser's default <details> behavior is now used.
 */
